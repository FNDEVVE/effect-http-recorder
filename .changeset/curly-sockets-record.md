---
"effect-http-recorder": minor
---

Add constructor-level WebSocket recording for applications that select connection URLs at runtime. Rename the public layers to `layer`, `layerSocket`, and `layerWebSocketConstructor` to mirror Effect's layer naming.
