---
"effect-http-recorder": patch
---

Fix Node 22 WebSocket replay, preserve unchanged JSON bodies during redaction, apply custom URL redaction to HTTP errors, and reject completely unused replay cassettes.
